model{
  b0~dnorm(0,0.01)
  b1~dnorm(0,0.01)
  sigma~dunif(0,30)
  tau<-1/pow(sigma,2)

  # obsErr<-9
  
  for(i in 1:n){
    yExp[i]<-b0+b1*x[i]
    # err[i]~dnorm(0,tau)
    y2[i]~dnorm(yExp[i],tau)
    y[i]~dsum(y2[i])
    resid[i]<-y[i]-yExp[i]
  }
}